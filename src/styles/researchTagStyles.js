import { createTheme } from '@mui/material/styles';

export const researchTagTheme = createTheme({
    
    palette: {
        tag: {
            blue: '#4285F4',
            red: '#EA4335',
            yellow: '#FBBC05',
            green: '#34A853'
        }
    }
});